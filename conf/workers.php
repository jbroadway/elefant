; <?php /*

; Use the list below to describe worker endpoints that should be
; started when `./elefant start-workers` is run. The keys are the
; worker paths to execute, which may include additional parameters.
; The values are the names of the workers, used only as a helpful
; reference here.

;jobqueue/worker = Example Worker

; */