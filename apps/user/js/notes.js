/**
 * Client-side helper for adding/removing notes.
 */
var notes = (function ($) {
	var self = {};
	
	self.notes = {};
	
	self.init = function (notes) {
		self.notes = notes;

		for (var i = 0; i < self.notes.length; i++) {
			
		}
	};

	return self;
})(jQuery);