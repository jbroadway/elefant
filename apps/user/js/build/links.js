/**
 * Client-side helper for adding/removing links.
 */
var links = (function ($) {
	var self = {};
	
	self.links = {};
	
	self.init = function (links) {
		self.links = links;

		for (var i = 0; i < self.links.length; i++) {
			
		}
	};

	return self;
})(jQuery);