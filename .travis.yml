language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

services: mongodb

before_script:
  - composer install --no-interaction --prefer-source

script:
  - lib/vendor/bin/phpunit --coverage-text --verbose

branches:
  only:
    - master
